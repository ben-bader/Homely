# Production profile overrides
# Disable Hibernate automatic schema updates in production to prevent schema drift.
# Flyway should manage schema migrations instead.
spring.jpa.hibernate.ddl-auto=validate
